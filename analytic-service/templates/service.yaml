apiVersion: v1
kind: Service
metadata:
  name: {{ include "analytic-service.fullname" . }}
  labels:
    {{- include "analytic-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "analytic-service.selectorLabels" . | nindent 4 }}
